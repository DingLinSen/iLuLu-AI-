services:
  - type: web
    name: ai-chat
    env: node
    buildCommand: npm install && npm run build
    startCommand: cd server && node index.js
    envVars:
      - key: NODE_VERSION
        value: 20.18.0
